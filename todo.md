- додати мініфікація, лейзі лоад, код спліт в проєкт
- on home page add list of movies user may like (based on favorite genres) +
- think about what I can add (new functionality):
- Add watchlist + show watchlist movies on user page +
- top rated movies page add load more to show up to 200 movies +
- use Use hook + suspense (for top rated movies)
- use useTransition with favorite toggle on movieDetailsPage (or find other places where to use)
- try to use useSyncExternalStore (if not needed find way to argument using it)

### docs
- loading optimizations (e.g., preload, preinit, prefetch) deep understanding (повторити) +
- підготуй список стратегій кешування і в чому їх сенс (повторити) 
- розібратись кеш контрол хедер (повторити)

# 09.07
typescript
prepare for next time:
- tsc compiler steps +
- type assertion +
- mapped types (with actual use case) +
- How does TypeScript handle function signatures in interfaces and type aliases? +
- function overload +

finished on Interfaces vs Types (Junior only)